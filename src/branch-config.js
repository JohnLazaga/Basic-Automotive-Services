/* Per-branch identity & URLs.
   ONE build per branch. build.js stamps the selected branch's entry from
   branches.json into the token below (see the replace in build.js). */
var BRANCH = __BRANCH_CONFIG__;
